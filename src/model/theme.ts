/**
 * A theme object that can be used to theme the UI.
 */
export interface Theme {
  /**
   * The user-friendly name of theme.
   */
  name: string

  /**
   * The computer-friendly name of theme.
   */
  key: string
}
